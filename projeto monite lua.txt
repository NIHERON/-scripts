--[[
    ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Hub v5 - Universal Roblox Script
    Key: unkteam
    Correções: Slider de speed não arrasta sem querer, speed padrão 200.
    Nova função: Assustar (cobre o mapa com skin e nome HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ)
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Debris = game:GetService("Debris")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- ================================================
--              CONFIGURAÇÕES E ESTADO
-- ================================================
local CORRECT_KEY = "unkteam"
local Settings = {
    AccentColor = Color3.fromRGB(150, 0, 255),
    AccentColor2 = Color3.fromRGB(0, 200, 255),
    FPSBoost = false,
    Fly = false,
    Noclip = false,
    Speed = false,
    God = false,
    Aimbot = false,
    SilentAim = false,
    Magnet = false,
    Spinbot = false,
    DoubleJump = false,
    AntiAFK = false,
    ESP = false,
    ESPBox = false,
    ESPLine = false,
    ESPHealth = false,
    ESPSkeleton = false,
    ESPName = false,
    ESPDistance = false,
    ESPRGB = false,
    Aura = false,
    KillAll = false,
    SpeedValue = 200, -- padrão 200
    AimbotFOV = 150,
    SpinSpeed = 15,
    FlySpeed = 60,
    AuraRadius = 40,
    AuraForce = 20000,
    Assustar = false,
}

local Connections = {}

-- ================================================
--              UTILITÁRIOS
-- ================================================
local function Create(class, props)
    local obj = Instance.new(class)
    for k, v in pairs(props) do
        if k ~= "Parent" then
            obj[k] = v
        end
    end
    if props.Parent then
        obj.Parent = props.Parent
    end
    return obj
end

local function MakeDraggable(frame, handle)
    local dragging = false
    local dragStart = nil
    local startPos = nil
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
end

local function GetClosestPlayer()
    local target = nil
    local closestDist = Settings.AimbotFOV
    local mousePos = UserInputService:GetMouseLocation()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Head") then
            local head = p.Character.Head
            local pos, vis = Camera:WorldToViewportPoint(head.Position)
            if vis then
                local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    target = p
                end
            end
        end
    end
    return target
end

local function GetCharacter()
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
end

-- ================================================
--              FPS BOOST / LAG REDUCER
-- ================================================
local function EnableFPSBoost()
    Settings.FPSBoost = true
    pcall(function()
        settings().Rendering.QualityLevel = 1
        settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Low
    end)
    pcall(function()
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 100
        Lighting.Brightness = 1
        Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
        Lighting.ClockTime = 12
    end)
    pcall(function()
        for _, v in pairs(Workspace:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") then
                v.Enabled = false
            end
            if v:IsA("Decal") or v:IsA("Texture") then
                v:Destroy()
            end
        end
    end)
    print("FPS Boost ativado")
end

local function DisableFPSBoost()
    Settings.FPSBoost = false
    pcall(function()
        settings().Rendering.QualityLevel = 7
        Lighting.GlobalShadows = true
        Lighting.FogEnd = 1000
    end)
    print("FPS Boost desativado")
end

-- ================================================
--              FLY SUAVE
-- ================================================
local flyBodyGyro = nil
local flyBodyVelocity = nil
local flyConnection = nil

local function EnableFly()
    local char = GetCharacter()
    local root = char:WaitForChild("HumanoidRootPart")
    local hum = char:WaitForChild("Humanoid")

    if not flyBodyGyro then
        flyBodyGyro = Instance.new("BodyGyro")
        flyBodyGyro.P = 9e4
        flyBodyGyro.MaxTorque = Vector3.new(9e4, 9e4, 9e4)
        flyBodyGyro.CFrame = root.CFrame
        flyBodyGyro.Parent = root
    end
    if not flyBodyVelocity then
        flyBodyVelocity = Instance.new("BodyVelocity")
        flyBodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
        flyBodyVelocity.Parent = root
    end

    hum.PlatformStand = true
    Settings.Fly = true

    flyConnection = RunService.RenderStepped:Connect(function()
        if not Settings.Fly then return end
        local moveDir = Vector3.new()
        local speed = Settings.FlySpeed

        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + root.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - root.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - root.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + root.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir = moveDir - Vector3.new(0, 1, 0) end

        local shift = UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) or UserInputService:IsKeyDown(Enum.KeyCode.RightShift)
        if shift then speed = Settings.FlySpeed * 2 end

        if moveDir.Magnitude > 0 then
            flyBodyVelocity.Velocity = moveDir.Unit * speed
            flyBodyGyro.CFrame = CFrame.new(root.Position, root.Position + moveDir.Unit)
        else
            flyBodyVelocity.Velocity = Vector3.new(0, 0, 0)
        end
    end)
end

local function DisableFly()
    Settings.Fly = false
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyBodyGyro then
        flyBodyGyro:Destroy()
        flyBodyGyro = nil
    end
    if flyBodyVelocity then
        flyBodyVelocity:Destroy()
        flyBodyVelocity = nil
    end
    local char = GetCharacter()
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.PlatformStand = false
    end
end

-- ================================================
--              NOCLIP
-- ================================================
local noclipConnection = nil
local function EnableNoclip()
    Settings.Noclip = true
    noclipConnection = RunService.Stepped:Connect(function()
        local char = GetCharacter()
        if char then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide then
                    part.CanCollide = false
                end
            end
        end
    end)
end

local function DisableNoclip()
    Settings.Noclip = false
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local char = GetCharacter()
    if char then
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
    end
end

-- ================================================
--              SPEED
-- ================================================
local speedConnection = nil
local function EnableSpeed()
    Settings.Speed = true
    speedConnection = RunService.RenderStepped:Connect(function()
        local char = GetCharacter()
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid.WalkSpeed = Settings.SpeedValue
        end
    end)
end

local function DisableSpeed()
    Settings.Speed = false
    if speedConnection then
        speedConnection:Disconnect()
        speedConnection = nil
    end
    local char = GetCharacter()
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.WalkSpeed = 16
    end
end

-- ================================================
--              GOD MODE
-- ================================================
local godConnection = nil
local function EnableGod()
    Settings.God = true
    godConnection = RunService.RenderStepped:Connect(function()
        local char = GetCharacter()
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid.Health = char.Humanoid.MaxHealth
            char.Humanoid.MaxHealth = math.huge
        end
    end)
end

local function DisableGod()
    Settings.God = false
    if godConnection then
        godConnection:Disconnect()
        godConnection = nil
    end
    local char = GetCharacter()
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.MaxHealth = 100
    end
end

-- ================================================
--              ESP SYSTEM
-- ================================================
local ESP_Objects = {}
local espConnection = nil

local function CreateESP(player)
    local objects = {
        BoxLines = {},
        Line = Drawing.new("Line"),
        Name = Drawing.new("Text"),
        Distance = Drawing.new("Text"),
        HealthBarBG = Drawing.new("Square"),
        HealthBar = Drawing.new("Square"),
        Skeleton = {},
    }
    for i = 1, 12 do
        local line = Drawing.new("Line")
        line.Thickness = 1.5
        line.Color = Color3.fromRGB(255,255,255)
        objects.BoxLines[i] = line
    end
    objects.Line.Thickness = 1.5
    objects.Line.Color = Color3.fromRGB(255,255,255)
    objects.Name.Size = 14
    objects.Name.Center = true
    objects.Name.Outline = true
    objects.Name.Color = Color3.fromRGB(255,255,255)
    objects.Distance.Size = 12
    objects.Distance.Center = true
    objects.Distance.Outline = true
    objects.Distance.Color = Color3.fromRGB(255,255,0)
    objects.HealthBarBG.Filled = true
    objects.HealthBarBG.Color = Color3.fromRGB(0,0,0)
    objects.HealthBar.Filled = true
    objects.HealthBar.Color = Color3.fromRGB(0,255,0)
    for i = 1, 15 do
        local line = Drawing.new("Line")
        line.Thickness = 2.5
        line.Color = Color3.fromRGB(255,255,255)
        objects.Skeleton[i] = line
    end
    ESP_Objects[player] = objects
end

local function UpdateESP()
    local hue = tick() % 5 / 5
    local rgbColor = Color3.fromHSV(hue, 1, 1)
    local espColor = Settings.ESPRGB and rgbColor or Color3.fromRGB(255,255,255)

    for player, objects in pairs(ESP_Objects) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local char = player.Character
            local hrp = char.HumanoidRootPart
            local hum = char.Humanoid
            local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position)

            if onScreen then
                if Settings.ESPBox then
                    local size = Vector3.new(2, 3, 2)
                    local cf = hrp.CFrame
                    local points = {
                        Camera:WorldToViewportPoint((cf * CFrame.new(size.X, size.Y, size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(-size.X, size.Y, size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(-size.X, -size.Y, size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(size.X, -size.Y, size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(size.X, size.Y, -size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(-size.X, size.Y, -size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(-size.X, -size.Y, -size.Z)).Position),
                        Camera:WorldToViewportPoint((cf * CFrame.new(size.X, -size.Y, -size.Z)).Position),
                    }
                    local edges = {{1,2},{2,3},{3,4},{4,1},{5,6},{6,7},{7,8},{8,5},{1,5},{2,6},{3,7},{4,8}}
                    for i, edge in pairs(edges) do
                        objects.BoxLines[i].Visible = true
                        objects.BoxLines[i].From = Vector2.new(points[edge[1]].X, points[edge[1]].Y)
                        objects.BoxLines[i].To = Vector2.new(points[edge[2]].X, points[edge[2]].Y)
                        objects.BoxLines[i].Color = espColor
                    end
                else
                    for _, l in pairs(objects.BoxLines) do l.Visible = false end
                end

                objects.Line.Visible = Settings.ESPLine
                if Settings.ESPLine then
                    objects.Line.From = Vector2.new(Camera.ViewportSize.X / 2, 0)
                    objects.Line.To = Vector2.new(pos.X, pos.Y)
                    objects.Line.Color = espColor
                end

                objects.HealthBarBG.Visible = Settings.ESPHealth
                objects.HealthBar.Visible = Settings.ESPHealth
                if Settings.ESPHealth then
                    local headPos = Camera:WorldToViewportPoint(char.Head.Position + Vector3.new(0, 0.5, 0))
                    local legPos = Camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3, 0))
                    local height = math.abs(headPos.Y - legPos.Y)
                    local hpPercent = hum.Health / hum.MaxHealth
                    objects.HealthBarBG.Size = Vector2.new(2, height)
                    objects.HealthBarBG.Position = Vector2.new(pos.X - 10, pos.Y - height/2)
                    objects.HealthBar.Size = Vector2.new(2, height * hpPercent)
                    objects.HealthBar.Position = Vector2.new(pos.X - 10, pos.Y + height/2 - (height * hpPercent))
                    objects.HealthBar.Color = Color3.fromRGB(255 * (1 - hpPercent), 255 * hpPercent, 0)
                end

                if Settings.ESPSkeleton and char:FindFirstChild("Humanoid") then
                    local parts = hum.RigType == Enum.HumanoidRigType.R15 and 
                        {{"Head","UpperTorso"},{"UpperTorso","LowerTorso"},{"UpperTorso","LeftUpperArm"},{"LeftUpperArm","LeftLowerArm"},{"UpperTorso","RightUpperArm"},{"RightUpperArm","RightLowerArm"},{"LowerTorso","LeftUpperLeg"},{"LeftUpperLeg","LeftLowerLeg"},{"LowerTorso","RightUpperLeg"},{"RightUpperLeg","RightLowerLeg"}} or
                        {{"Head","Torso"},{"Torso","Left Arm"},{"Torso","Right Arm"},{"Torso","Left Leg"},{"Torso","Right Leg"}}
                    for i, p in pairs(parts) do
                        local line = objects.Skeleton[i]
                        if char:FindFirstChild(p[1]) and char:FindFirstChild(p[2]) then
                            local p1, v1 = Camera:WorldToViewportPoint(char[p[1]].Position)
                            local p2, v2 = Camera:WorldToViewportPoint(char[p[2]].Position)
                            line.Visible = v1 and v2
                            line.From = Vector2.new(p1.X, p1.Y)
                            line.To = Vector2.new(p2.X, p2.Y)
                            line.Color = espColor
                        else
                            line.Visible = false
                        end
                    end
                else
                    for _, l in pairs(objects.Skeleton) do l.Visible = false end
                end

                objects.Name.Visible = Settings.ESPName
                objects.Name.Text = player.Name
                objects.Name.Position = Vector2.new(pos.X, pos.Y - 40)
                objects.Name.Color = Color3.fromRGB(255,255,255)

                objects.Distance.Visible = Settings.ESPDistance
                local dist = math.floor((hrp.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude)
                objects.Distance.Text = "["..dist.."m]"
                objects.Distance.Position = Vector2.new(pos.X, pos.Y + 40)
                objects.Distance.Color = Color3.fromRGB(255,255,0)
            else
                for _, v in pairs(objects) do
                    if type(v) == "table" then for _, l in pairs(v) do l.Visible = false end else v.Visible = false end
                end
            end
        else
            for _, v in pairs(objects) do
                if type(v) == "table" then for _, l in pairs(v) do l.Visible = false end else v.Visible = false end
            end
        end
    end
end

local function EnableESP()
    Settings.ESP = true
    if not espConnection then
        espConnection = RunService.RenderStepped:Connect(UpdateESP)
    end
end

local function DisableESP()
    Settings.ESP = false
    if espConnection then
        espConnection:Disconnect()
        espConnection = nil
    end
    for _, objects in pairs(ESP_Objects) do
        for _, v in pairs(objects) do
            if type(v) == "table" then for _, l in pairs(v) do l.Visible = false end else v.Visible = false end
        end
    end
end

-- ================================================
--              AIMBOT & SILENT AIM
-- ================================================
local aimbotConnection = nil
local function EnableAimbot()
    Settings.Aimbot = true
    aimbotConnection = RunService.RenderStepped:Connect(function()
        if Settings.Aimbot and UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
            local target = GetClosestPlayer()
            if target and target.Character and target.Character:FindFirstChild("Head") then
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Character.Head.Position)
            end
        end
    end)
end

local function DisableAimbot()
    Settings.Aimbot = false
    if aimbotConnection then
        aimbotConnection:Disconnect()
        aimbotConnection = nil
    end
end

local silentAimConnection = nil
local function EnableSilentAim()
    Settings.SilentAim = true
    silentAimConnection = RunService.RenderStepped:Connect(function()
        if Settings.SilentAim and UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
            local target = GetClosestPlayer()
            if target and target.Character and target.Character:FindFirstChild("Head") then
                local screenPos = Camera:WorldToViewportPoint(target.Character.Head.Position)
                local mousePos = UserInputService:GetMouseLocation()
                local delta = Vector2.new(screenPos.X, screenPos.Y) - mousePos
                mousemoverel(delta.X, delta.Y)
            end
        end
    end)
end

local function DisableSilentAim()
    Settings.SilentAim = false
    if silentAimConnection then
        silentAimConnection:Disconnect()
        silentAimConnection = nil
    end
end

-- ================================================
--              MAGNET
-- ================================================
local magnetConnection = nil
local function EnableMagnet()
    Settings.Magnet = true
    magnetConnection = RunService.RenderStepped:Connect(function()
        if Settings.Magnet and UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
            local target = GetClosestPlayer()
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                local playerRoot = GetCharacter():FindFirstChild("HumanoidRootPart")
                if playerRoot then
                    target.Character.HumanoidRootPart.CFrame = playerRoot.CFrame * CFrame.new(0, 0, -8)
                end
            end
        end
    end)
end

local function DisableMagnet()
    Settings.Magnet = false
    if magnetConnection then
        magnetConnection:Disconnect()
        magnetConnection = nil
    end
end

-- ================================================
--              SPINBOT
-- ================================================
local spinConnection = nil
local function EnableSpinbot()
    Settings.Spinbot = true
    spinConnection = RunService.RenderStepped:Connect(function()
        local char = GetCharacter()
        if Settings.Spinbot and char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(Settings.SpinSpeed), 0)
        end
    end)
end

local function DisableSpinbot()
    Settings.Spinbot = false
    if spinConnection then
        spinConnection:Disconnect()
        spinConnection = nil
    end
end

-- ================================================
--              DOUBLE JUMP
-- ================================================
local djConnection = nil
local canDoubleJump = true
local function EnableDoubleJump()
    Settings.DoubleJump = true
    djConnection = UserInputService.JumpRequest:Connect(function()
        if Settings.DoubleJump then
            local char = GetCharacter()
            if char and char:FindFirstChild("Humanoid") and char.Humanoid.FloorMaterial == Enum.Material.Air then
                if canDoubleJump then
                    char.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                    canDoubleJump = false
                    wait(1)
                    canDoubleJump = true
                end
            end
        end
    end)
end

local function DisableDoubleJump()
    Settings.DoubleJump = false
    if djConnection then
        djConnection:Disconnect()
        djConnection = nil
    end
end

-- ================================================
--              ANTI AFK
-- ================================================
local afkConnection = nil
local function EnableAntiAFK()
    Settings.AntiAFK = true
    afkConnection = RunService.RenderStepped:Connect(function()
        if Settings.AntiAFK then
            local char = GetCharacter()
            if char and char:FindFirstChild("Humanoid") then
                char.Humanoid:Move(Vector3.new(math.random(-1,1), 0, math.random(-1,1)), false)
            end
        end
    end)
end

local function DisableAntiAFK()
    Settings.AntiAFK = false
    if afkConnection then
        afkConnection:Disconnect()
        afkConnection = nil
    end
end

-- ================================================
--              AURA (PUXAR BLOCOS)
-- ================================================
local auraConnection = nil
local function EnableAura()
    Settings.Aura = true
    auraConnection = RunService.RenderStepped:Connect(function()
        if not Settings.Aura then return end
        local char = GetCharacter()
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local center = root.Position
        for _, obj in pairs(Workspace:GetDescendants()) do
            if obj:IsA("BasePart") and obj ~= root and obj.Parent ~= char and not obj.Anchored then
                local dist = (obj.Position - center).Magnitude
                if dist <= Settings.AuraRadius then
                    pcall(function()
                        local force = Instance.new("BodyPosition")
                        force.Position = center + Vector3.new(0, 5, 0)
                        force.MaxForce = Vector3.new(Settings.AuraForce, Settings.AuraForce, Settings.AuraForce)
                        force.P = 10000
                        force.D = 500
                        force.Parent = obj
                        Debris:AddItem(force, 0.1)
                    end)
                end
            end
        end
    end)
end

local function DisableAura()
    Settings.Aura = false
    if auraConnection then
        auraConnection:Disconnect()
        auraConnection = nil
    end
end

-- ================================================
--              KILL ALL
-- ================================================
local function KillAll()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Humanoid") then
            p.Character.Humanoid.Health = 0
        end
    end
end

-- ================================================
--              ASSUSTAR (NOVA FUNÇÃO)
-- ================================================
local function Assustar()
    Settings.Assustar = true
    print("Assustar ativado! Tentando replicar...")
    
    -- Tenta obter textura da cara do jogador
    local faceTexture = ""
    local char = GetCharacter()
    if char and char:FindFirstChild("Head") then
        local head = char.Head
        if head:FindFirstChild("face") then
            faceTexture = head.face.Texture
        end
    end
    
    -- Se não tiver textura, usa vermelho
    local decalTexture = faceTexture ~= "" and faceTexture or "rbxassetid://0"
    
    -- Cria partes gigantes para chão, paredes e céu
    local function createPart(name, size, position, color)
        local part = Instance.new("Part")
        part.Name = name
        part.Size = size
        part.Position = position
        part.Anchored = true
        part.CanCollide = false
        part.Material = Enum.Material.Neon
        part.BrickColor = BrickColor.new(color)
        
        -- Adiciona Decal com a textura da cara
        local decal = Instance.new("Decal")
        decal.Texture = decalTexture
        decal.Face = Enum.NormalId.Front
        decal.Parent = part
        
        -- Adiciona um TextLabel? Nome visível? Para todos? Talvez via BillboardGui?
        local billboard = Instance.new("BillboardGui")
        billboard.Size = UDim2.new(0, 200, 0, 50)
        billboard.Parent = part
        billboard.Adornee = part
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.Text = "HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ"
        label.TextColor3 = Color3.fromRGB(255,0,0)
        label.Font = Enum.Font.GothamBlack
        label.TextSize = 30
        label.Parent = billboard
        
        part.Parent = Workspace
    end
    
    -- Chão
    createPart("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Floor", Vector3.new(1000, 1, 1000), Vector3.new(0, -2, 0), Color3.fromRGB(255,0,0))
    -- Paredes (4 lados)
    createPart("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Wall1", Vector3.new(1000, 100, 1), Vector3.new(0, 50, -500), Color3.fromRGB(0,0,255))
    createPart("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Wall2", Vector3.new(1000, 100, 1), Vector3.new(0, 50, 500), Color3.fromRGB(0,255,0))
    createPart("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Wall3", Vector3.new(1, 100, 1000), Vector3.new(-500, 50, 0), Color3.fromRGB(255,255,0))
    createPart("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Wall4", Vector3.new(1, 100, 1000), Vector3.new(500, 50, 0), Color3.fromRGB(255,0,255))
    -- Céu
    createPart("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Sky", Vector3.new(1000, 1, 1000), Vector3.new(0, 200, 0), Color3.fromRGB(0,0,0))
    
    -- Tenta replicar via remotos (fire server)
    spawn(function()
        for _, v in pairs(Workspace:GetDescendants()) do
            if v:IsA("RemoteEvent") then
                pcall(function()
                    v:FireServer("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ", decalTexture, "createPart")
                end)
            elseif v:IsA("RemoteFunction") then
                pcall(function()
                    v:InvokeServer("HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ", decalTexture, "createPart")
                end)
            end
        end
    end)
end

local function DisableAssustar()
    Settings.Assustar = false
    -- Remove as partes criadas
    for _, part in pairs(Workspace:GetChildren()) do
        if part:IsA("Part") and string.sub(part.Name, 1, 13) == "HACKED BY ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ" then
            part:Destroy()
        end
    end
end

-- ================================================
--              UI CONSTRUCTION
-- ================================================
local ScreenGui = Create("ScreenGui", {Name = "ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ HubV5", Parent = game.CoreGui, ZIndexBehavior = Enum.ZIndexBehavior.Global})

-- Key System
local KeyFrame = Create("Frame", {Size = UDim2.new(0, 400, 0, 250), Position = UDim2.new(0.5, -200, 0.5, -125), BackgroundColor3 = Color3.fromRGB(20,20,25), ZIndex = 100, Parent = ScreenGui})
Create("UICorner", {CornerRadius = UDim.new(0, 20), Parent = KeyFrame})
Create("UIStroke", {Thickness = 3, Color = Settings.AccentColor, Parent = KeyFrame})
local KeyTitle = Create("TextLabel", {Text = "ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ HUB - KEY", Size = UDim2.new(1,0,0,50), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBlack, TextSize = 22, ZIndex = 101, Parent = KeyFrame})
local KeyInput = Create("TextBox", {PlaceholderText = "Key...", Size = UDim2.new(0.8,0,0,40), Position = UDim2.new(0.1,0,0.4,0), BackgroundColor3 = Color3.fromRGB(40,40,50), TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.Gotham, ZIndex = 101, Parent = KeyFrame})
Create("UICorner", {CornerRadius = UDim.new(0, 10), Parent = KeyInput})
local KeyBtn = Create("TextButton", {Text = "ENTRAR", Size = UDim2.new(0.6,0,0,40), Position = UDim2.new(0.2,0,0.7,0), BackgroundColor3 = Settings.AccentColor, TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, ZIndex = 101, Parent = KeyFrame})
Create("UICorner", {CornerRadius = UDim.new(0, 10), Parent = KeyBtn})

-- Main Frame
local MainFrame = Create("Frame", {Size = UDim2.new(0, 600, 0, 420), Position = UDim2.new(0.5, -300, 0.5, -210), BackgroundColor3 = Color3.fromRGB(15,15,20), Visible = false, ZIndex = 10, Parent = ScreenGui})
Create("UICorner", {CornerRadius = UDim.new(0, 20), Parent = MainFrame})
local MainStroke = Create("UIStroke", {Thickness = 3, Color = Settings.AccentColor, Parent = MainFrame})

local FloatingBtn = Create("TextButton", {Size = UDim2.new(0, 60, 0, 60), Position = UDim2.new(0, 10, 0.5, -30), BackgroundColor3 = Settings.AccentColor, Text = "MH", TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBlack, TextSize = 20, Visible = false, ZIndex = 20, Parent = ScreenGui})
Create("UICorner", {CornerRadius = UDim.new(1, 0), Parent = FloatingBtn})
Create("UIStroke", {Thickness = 2, Color = Color3.fromRGB(255,255,255), Parent = FloatingBtn})
MakeDraggable(FloatingBtn, FloatingBtn)
FloatingBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

KeyBtn.MouseButton1Click:Connect(function()
    if KeyInput.Text == CORRECT_KEY then
        KeyFrame.Visible = false
        MainFrame.Visible = true
        FloatingBtn.Visible = true
    else
        KeyInput.Text = ""
        KeyInput.PlaceholderText = "Key errada!"
    end
end)

-- Sidebar
local Sidebar = Create("Frame", {Size = UDim2.new(0, 80, 1, 0), BackgroundColor3 = Color3.fromRGB(20,20,30), ZIndex = 11, Parent = MainFrame})
Create("UICorner", {CornerRadius = UDim.new(0, 20), Parent = Sidebar})
local IconContainer = Create("Frame", {Size = UDim2.new(1,0,1,-40), Position = UDim2.new(0,0,0,20), BackgroundTransparency = 1, ZIndex = 12, Parent = Sidebar})
Create("UIListLayout", {Parent = IconContainer, HorizontalAlignment = Enum.HorizontalAlignment.Center, Padding = UDim.new(0, 15)})

local MoonBtn = Create("TextButton", {Text = "🌙", Size = UDim2.new(0, 40, 0, 40), Position = UDim2.new(1, -50, 0, 5), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 25, ZIndex = 15, Parent = MainFrame})
MoonBtn.MouseButton1Click:Connect(function()
    local newAccent = Settings.AccentColor == Color3.fromRGB(150,0,255) and Color3.fromRGB(255,0,0) or Color3.fromRGB(150,0,255)
    Settings.AccentColor = newAccent
    MainStroke.Color = newAccent
    FloatingBtn.BackgroundColor3 = newAccent
    KeyBtn.BackgroundColor3 = newAccent
    MoonBtn.TextColor3 = newAccent
end)

local TabContainer = Create("Frame", {Size = UDim2.new(1, -90, 1, -60), Position = UDim2.new(0, 80, 0, 50), BackgroundTransparency = 1, ZIndex = 11, Parent = MainFrame})
local TabFrames = {}

local function AddTab(name, icon, order)
    local btn = Create("TextButton", {Text = icon, Size = UDim2.new(0, 50, 0, 50), BackgroundColor3 = (order == 1) and Settings.AccentColor or Color3.fromRGB(30,30,40), TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 22, ZIndex = 13, Parent = IconContainer})
    Create("UICorner", {CornerRadius = UDim.new(1, 0), Parent = btn})
    local frame = Create("ScrollingFrame", {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Visible = (order == 1), ZIndex = 13, Parent = TabContainer, ScrollBarThickness = 3})
    Create("UIListLayout", {Parent = frame, Padding = UDim.new(0, 10)})
    TabFrames[name] = {frame = frame, btn = btn}
    btn.MouseButton1Click:Connect(function()
        for _, t in pairs(TabFrames) do
            t.frame.Visible = false
            t.btn.BackgroundColor3 = Color3.fromRGB(30,30,40)
        end
        frame.Visible = true
        btn.BackgroundColor3 = Settings.AccentColor
    end)
    return frame
end

local ToggleObjects = {}
local function AddToggle(parent, text, defaultState, callback)
    local row = Create("Frame", {Size = UDim2.new(1, 0, 0, 45), BackgroundColor3 = Color3.fromRGB(25,25,35), ZIndex = 14, Parent = parent})
    Create("UICorner", {CornerRadius = UDim.new(0, 12), Parent = row})
    local label = Create("TextLabel", {Text = text, Size = UDim2.new(0.7, 0, 1, 0), Position = UDim2.new(0, 15, 0, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.Gotham, TextSize = 14, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 15, Parent = row})
    local toggle = Create("TextButton", {Text = "", Size = UDim2.new(0, 40, 0, 20), Position = UDim2.new(1, -50, 0.5, -10), BackgroundColor3 = defaultState and Settings.AccentColor or Color3.fromRGB(50,50,60), ZIndex = 15, Parent = row})
    Create("UICorner", {CornerRadius = UDim.new(1, 0), Parent = toggle})
    local state = defaultState or false
    local function update()
        toggle.BackgroundColor3 = state and Settings.AccentColor or Color3.fromRGB(50,50,60)
        label.TextColor3 = state and Settings.AccentColor2 or Color3.fromRGB(255,255,255)
    end
    toggle.MouseButton1Click:Connect(function()
        state = not state
        update()
        callback(state)
    end)
    update()
    table.insert(ToggleObjects, {label = label, toggle = toggle, state = state})
    return {SetState = function(v) state = v; update(); callback(v) end}
end

-- Slider FIX: só move quando clica na barrinha
local function AddSlider(parent, text, min, max, default, callback)
    local row = Create("Frame", {Size = UDim2.new(1, 0, 0, 60), BackgroundColor3 = Color3.fromRGB(25,25,35), ZIndex = 14, Parent = parent})
    Create("UICorner", {CornerRadius = UDim.new(0, 12), Parent = row})
    local label = Create("TextLabel", {Text = text..": "..default, Size = UDim2.new(1, 0, 0, 25), Position = UDim2.new(0, 15, 0, 0), BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.Gotham, TextSize = 13, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 15, Parent = row})
    local sliderBg = Create("Frame", {Size = UDim2.new(0.9, 0, 0, 6), Position = UDim2.new(0.05, 0, 0.7, 0), BackgroundColor3 = Color3.fromRGB(50,50,60), ZIndex = 15, Parent = row})
    Create("UICorner", {CornerRadius = UDim.new(1, 0), Parent = sliderBg})
    local sliderFill = Create("Frame", {Size = UDim2.new((default-min)/(max-min), 0, 1, 0), BackgroundColor3 = Settings.AccentColor, ZIndex = 16, Parent = sliderBg})
    Create("UICorner", {CornerRadius = UDim.new(1, 0), Parent = sliderFill})
    local sliderBtn = Create("TextButton", {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Text = "", ZIndex = 17, Parent = sliderBg})

    local isDraggingSlider = false
    sliderBtn.MouseButton1Down:Connect(function()
        isDraggingSlider = true
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            isDraggingSlider = false
        end
    end)

    RunService.RenderStepped:Connect(function()
        if isDraggingSlider then
            local mouseX = UserInputService:GetMouseLocation().X
            local rel = math.clamp((mouseX - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
            sliderFill.Size = UDim2.new(rel, 0, 1, 0)
            local val = math.floor(min + (max - min) * rel)
            label.Text = text..": "..val
            callback(val)
        end
    end)
end

-- ================================================
--              PREENCHER ABAS
-- ================================================
local MainTab = AddTab("Main", "🎯", 1)
AddToggle(MainTab, "FPS Boost", false, function(v) if v then EnableFPSBoost() else DisableFPSBoost() end end)
AddToggle(MainTab, "Fly", false, function(v) if v then EnableFly() else DisableFly() end end)
AddToggle(MainTab, "Noclip", false, function(v) if v then EnableNoclip() else DisableNoclip() end end)
AddToggle(MainTab, "Speed", false, function(v) if v then EnableSpeed() else DisableSpeed() end end)
AddToggle(MainTab, "God Mode", false, function(v) if v then EnableGod() else DisableGod() end end)
AddSlider(MainTab, "Speed Value", 16, 500, Settings.SpeedValue, function(v) Settings.SpeedValue = v end)
AddSlider(MainTab, "Fly Speed", 10, 200, Settings.FlySpeed, function(v) Settings.FlySpeed = v end)

local VisualTab = AddTab("Visual", "👁️", 2)
AddToggle(VisualTab, "ESP Master", false, function(v) Settings.ESP = v; if v then EnableESP() else DisableESP() end end)
AddToggle(VisualTab, "ESP Box 3D", false, function(v) Settings.ESPBox = v end)
AddToggle(VisualTab, "ESP Line", false, function(v) Settings.ESPLine = v end)
AddToggle(VisualTab, "ESP Health", false, function(v) Settings.ESPHealth = v end)
AddToggle(VisualTab, "ESP Skeleton", false, function(v) Settings.ESPSkeleton = v end)
AddToggle(VisualTab, "ESP Name", false, function(v) Settings.ESPName = v end)
AddToggle(VisualTab, "ESP Distance", false, function(v) Settings.ESPDistance = v end)
AddToggle(VisualTab, "ESP RGB", false, function(v) Settings.ESPRGB = v end)

local LegitTab = AddTab("Legit", "🎯", 3)
AddToggle(LegitTab, "Aimbot", false, function(v) if v then EnableAimbot() else DisableAimbot() end end)
AddToggle(LegitTab, "Silent Aim", false, function(v) if v then EnableSilentAim() else DisableSilentAim() end end)
AddToggle(LegitTab, "Magnet", false, function(v) if v then EnableMagnet() else DisableMagnet() end end)
AddSlider(LegitTab, "Aimbot FOV", 10, 500, Settings.AimbotFOV, function(v) Settings.AimbotFOV = v end)

local PlayerTab = AddTab("Player", "🧍", 4)
AddToggle(PlayerTab, "Spinbot", false, function(v) if v then EnableSpinbot() else DisableSpinbot() end end)
AddToggle(PlayerTab, "Double Jump", false, function(v) if v then EnableDoubleJump() else DisableDoubleJump() end end)
AddToggle(PlayerTab, "Anti AFK", false, function(v) if v then EnableAntiAFK() else DisableAntiAFK() end end)
AddToggle(PlayerTab, "Aura (Puxar)", false, function(v) if v then EnableAura() else DisableAura() end end)
AddSlider(PlayerTab, "Spin Speed", 5, 100, Settings.SpinSpeed, function(v) Settings.SpinSpeed = v end)
AddSlider(PlayerTab, "Aura Radius", 10, 100, Settings.AuraRadius, function(v) Settings.AuraRadius = v end)

local MiscTab = AddTab("Misc", "⚙️", 5)
-- Botão Assustar
local AssustarBtn = Create("TextButton", {Text = "Assustar", Size = UDim2.new(1, -20, 0, 45), BackgroundColor3 = Color3.fromRGB(200,50,50), TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 16, ZIndex = 15, Parent = MiscTab})
Create("UICorner", {CornerRadius = UDim.new(0, 12), Parent = AssustarBtn})
AssustarBtn.MouseButton1Click:Connect(function()
    if Settings.Assustar then
        DisableAssustar()
        AssustarBtn.Text = "Assustar"
    else
        Assustar()
        AssustarBtn.Text = "Assustar (ATIVO)"
    end
end)

local KillAllBtn = Create("TextButton", {Text = "Kill All", Size = UDim2.new(1, -20, 0, 45), BackgroundColor3 = Color3.fromRGB(200,50,50), TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.GothamBold, TextSize = 16, ZIndex = 15, Parent = MiscTab})
Create("UICorner", {CornerRadius = UDim.new(0, 12), Parent = KillAllBtn})
KillAllBtn.MouseButton1Click:Connect(KillAll)

-- Teleport list
local TeleportFrame = Create("Frame", {Size = UDim2.new(1, -20, 0, 200), BackgroundColor3 = Color3.fromRGB(30,30,40), ZIndex = 15, Parent = MiscTab})
Create("UICorner", {CornerRadius = UDim.new(0, 12), Parent = TeleportFrame})
Create("UIListLayout", {Parent = TeleportFrame, Padding = UDim.new(0, 5)})
local function RefreshTeleportList()
    for _, child in pairs(TeleportFrame:GetChildren()) do if child:IsA("TextButton") then child:Destroy() end end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local btn = Create("TextButton", {Text = "TP: "..p.Name, Size = UDim2.new(1, -10, 0, 30), BackgroundColor3 = Color3.fromRGB(60,60,70), TextColor3 = Color3.fromRGB(255,255,255), Font = Enum.Font.Gotham, TextSize = 13, ZIndex = 16, Parent = TeleportFrame})
            Create("UICorner", {CornerRadius = UDim.new(0, 8), Parent = btn})
            btn.MouseButton1Click:Connect(function() TeleportToPlayer(p) end)
        end
    end
end
Players.PlayerAdded:Connect(RefreshTeleportList)
Players.PlayerRemoving:Connect(RefreshTeleportList)
RefreshTeleportList()

-- Inicializar ESP
for _, p in pairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then CreateESP(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p ~= LocalPlayer then CreateESP(p) end
end)

print("ᴠͥɪͣᴘͫ | ɴɪʜᴇʀᴏɴ ᴏғᴄ Hub v5 carregado! Key: unkteam")